#!/usr/bin/env sh
set -e
echo "[husky] pre-push: lint + format:check + e2e (apps/web)"
cd "$(git rev-parse --show-toplevel)/apps/web"
npm run lint && npm run format:check && npm run test:e2e:headless
