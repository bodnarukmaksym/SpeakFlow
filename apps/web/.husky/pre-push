#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
set -e

echo "[husky] pre-push: eslint + prettier check (apps/web)"
cd "$(git rev-parse --show-toplevel)/apps/web"

npm run lint:ci || npm run lint --if-present
npm run format:ci || npm run format:check --if-present
